apiVersion: v1
kind: Service
metadata:
  name: {{ template "debtco-chart.fullname" . }}
  labels:
    app: {{ template "debtco-chart.fullname" . }}
    chart: {{ template "debtco-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "debtco-chart.fullname" . }}
    release: {{ .Release.Name }}
