{{- $root := . }}
{{- range $name, $values := .Values.secrets }}
apiVersion: v2
kind: Secret
metadata:
  name: {{ template "microservice-chart.fullname" $root }}-{{ $name | lower }}
data:
  {{- range $key, $value := $values }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
---
{{- end }}
